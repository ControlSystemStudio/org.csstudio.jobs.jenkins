#
#
#
#

- project:
    name: base
    top: core
    version: 
            - master
            - 4.2.x
    jobs:
        - 'cs-studio-{top}-{name}-{version}'
- project:
    name: platform
    top: core
    version: 
            - master
            - 4.2.x
    jobs:
        - 'cs-studio-{top}-{name}-{version}'
- project:
    name: ui
    top: core
    version: 
            - master
            - 4.2.x
    jobs:
        - 'cs-studio-{top}-{name}-{version}'
- project:
    name: unorganized
    top: core
    version: 
            - master
            - 4.2.x
    jobs:
        - 'cs-studio-{top}-{name}-{version}'
- project:
    name: utility
    top: core
    version: 
            - master
            - 4.2.x
    jobs:
        - 'cs-studio-{top}-{name}-{version}'

- project:
    name: diirt
    top: core
    version: 
            - master
            - 4.2.x
    jobs:
        - 'cs-studio-{top}-{name}-{version}'
